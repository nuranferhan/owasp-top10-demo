<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results - Vulnerable Blog</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="nav-container">
            <h1><a href="/">Vulnerable Blog</a></h1>
            <div class="nav-links">
                <a href="/">Home</a>
                <% if (user) { %>
                    <a href="/dashboard">Dashboard</a>
                    <a href="/logout">Logout</a>
                <% } else { %>
                    <a href="/login">Login</a>
                <% } %>
            </div>
        </div>
    </nav>

    <div class="container">
        <h2>Search Results</h2>
        
        <div class="search-box">
            <form action="/search" method="GET">
                <input type="text" name="q" placeholder="Search posts..." value="<%= searchTerm %>">
                <button type="submit">Search</button>
            </form>
        </div>
        
        <% if (searchTerm) { %>
            <p>Search results for: <strong><%- searchTerm %></strong></p>
            
            <div class="vulnerable-features">
                <h4>⚠️ Reflected XSS Vulnerability:</h4>
                <p>Search term is reflected without sanitization. Try:</p>
                <ul>
                    <li><code>&lt;script&gt;alert('XSS')&lt;/script&gt;</code></li>
                    <li><code>&lt;img src=x onerror=alert('XSS')&gt;</code></li>
                    <li><code>&lt;svg onload=alert('XSS')&gt;</code></li>
                </ul>
            </div>
        <% } %>
        
        <div class="posts">
            <% if (posts && posts.length > 0) { %>
                <% posts.forEach(post => { %>
                    <div class="post">
                        <h3><a href="/post/<%= post.id %>"><%= post.title %></a></h3>
                        <p><%= post.content.substring(0, 200) %>...</p>
                        <small>By User ID: <%= post.author_id %> on <%= post.created_at %></small>
                    </div>
                <% }); %>
            <% } else { %>
                <p>No posts found matching your search.</p>
            <% } %>
        </div>
    </div>
</body>
</html>
